This repository contains three small HTML/CSS projects built to practice semantic markup, responsive design, 
and modern CSS layout techniques (Grid, Flexbox, and sticky positioning).
All projects are fully responsive, accessible, and use no JavaScript.

📌 Project 1 - Sticky Header
A responsive page containing a sticky header. 
Key Features
  Semantic HTML: <header>, <ul>, <nav>
  Nav divided into three different sections.
  Long Scroll length to see sticky header


📌 Project 2 — Product Cards Grid
  A responsive grid of product cards with one featured card spanning the full width on larger screens.
Key Features
  Semantic HTML: <section>, <article>, <figure>, <img>, <figcaption>.
  CSS Grid layout using repeat(auto-fit, minmax()).
  Featured card spans all columns on desktop.
  Clean stacking to one column on mobile.


📌 Project 3 — Article Page with Sticky TOC
  A content page with a main article and a right sidebar Table of Contents (TOC) that stays visible while scrolling.
Key Features
  Semantic structure with <main>, <article>, <nav> and anchors.
  Two-column layout using CSS Grid.
  Sticky TOC using position: sticky; top: 1rem;.
  Smooth section scrolling (scroll-behavior: smooth).
  Responsive single-column layout on mobile.

  🛠 Technologies Used
    HTML5
    CSS3
    Grid Layout
    Custom properties (var())
    Sticky positioning
